Comparing files JQUERY\RD\52.js and JQUERY\RD\ORIGINAL.JS
***** JQUERY\RD\52.js
  437:              // Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
  438:              rwhitespace = new RegExp(whitespace + '+'), rtrim = new RegExp('^' + whitespace + '+|((?:^|[^\\\\])(?:\\\\.)*)' + w
  439:  hitespace + '+$', 'g'), rcomma = new RegExp('^' + whitespace + '*,' + whitespace + '*'), rcombinators = new RegExp('^' + whites
  440:  pace + '*([>+~]|' + whitespace + ')' + whitespace + '*'), rattributeQuotes = new RegExp('=' + whitespace + '*([^\\]\'"]*?)' + w
  441:  hitespace + '*\\]', 'g'), rpseudo = new RegExp(pseudos), ridentifier = new RegExp('^' + identifier + '$'), matchExpr = {
  442:                  'ID': new RegExp('^#(' + identifier + ')'),
***** JQUERY\RD\ORIGINAL.JS
  437:              // Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
  438:              rwhitespace = new RegExp(whitespace + '+', 'g'), rtrim = new RegExp('^' + whitespace + '+|((?:^|[^\\\\])(?:\\\\.)*)
  439:  ' + whitespace + '+$', 'g'), rcomma = new RegExp('^' + whitespace + '*,' + whitespace + '*'), rcombinators = new RegExp('^' + w
  440:  hitespace + '*([>+~]|' + whitespace + ')' + whitespace + '*'), rattributeQuotes = new RegExp('=' + whitespace + '*([^\\]\'"]*?)
  441:  ' + whitespace + '*\\]', 'g'), rpseudo = new RegExp(pseudos), ridentifier = new RegExp('^' + identifier + '$'), matchExpr = {
  442:                  'ID': new RegExp('^#(' + identifier + ')'),
*****

***** JQUERY\RD\52.js
  720:  k non-disabled form elements for fieldset[disabled] ancestors
  721:                  'form' in elem && elem.disabled === false && (elem.isDisabled === disabled || // Otherwise, assume any non-<opt
  722:  ion> under fieldset[disabled] is disabled
  723:                  /* jshint -W018 */
***** JQUERY\RD\ORIGINAL.JS
  720:  k non-disabled form elements for fieldset[disabled] ancestors
  721:                  'form' in elem && elem.disabled === false && // Support: IE6-11+
  722:                  // Ancestry is covered for us
  723:                  (elem.isDisabled === disabled || // Otherwise, assume any non-<option> under fieldset[disabled] is disabled
  724:                  /* jshint -W018 */
*****

***** JQUERY\RD\52.js
 2930:                      } else {
 2931:                          Object.defineProperty(owner, this.expando, { value: value });
 2932:                      }
***** JQUERY\RD\ORIGINAL.JS
 2931:                      } else {
 2932:                          Object.defineProperty(owner, this.expando, {
 2933:                              value: value,
 2934:                              configurable: true
 2935:                          });
 2936:                      }
*****

***** JQUERY\RD\52.js
 3159:              type = type || 'fx';
 3160:              var queue = jQuery.queue(elem, type), startLength = queue.length, fn = queue.shift(), hooks, next = function () {
 3161:                      jQuery.dequeue(elem, type);
***** JQUERY\RD\ORIGINAL.JS
 3163:              type = type || 'fx';
 3164:              var queue = jQuery.queue(elem, type), startLength = queue.length, fn = queue.shift(), hooks = jQuery._queueHooks(el
 3165:  em, type), next = function () {
 3166:                      jQuery.dequeue(elem, type);
*****

***** JQUERY\RD\52.js
 4614:                  val += jQuery.css(elem, 'padding' + cssExpand[i], true, styles);
 4615:              }
***** JQUERY\RD\ORIGINAL.JS
 4619:                  val += jQuery.css(elem, 'padding' + cssExpand[i], true, styles);
 4620:                  // At this point, extra isn't content nor padding, so add border
 4621:                  if (extra !== 'padding') {
 4622:                      val += jQuery.css(elem, 'border' + cssExpand[i] + 'Width', true, styles);
 4623:                  }
 4624:              }
*****

***** JQUERY\RD\52.js
 5181:                      animation.tweens.push(tween);
 5182:                  },
***** JQUERY\RD\ORIGINAL.JS
 5190:                      animation.tweens.push(tween);
 5191:                      return tween;
 5192:                  },
*****

***** JQUERY\RD\52.js
 5194:                      }
 5195:                      if (gotoEnd) {
***** JQUERY\RD\ORIGINAL.JS
 5204:                      }
 5205:                      // Resolve when we played the last frame; otherwise, reject
 5206:                      if (gotoEnd) {
*****

***** JQUERY\RD\52.js
 5760:                      classNames = value.match(rnotwhite) || [];
 5761:                  } else if (value === undefined || type === 'boolean') {
***** JQUERY\RD\ORIGINAL.JS
 5771:                      classNames = value.match(rnotwhite) || [];
 5772:                      while (className = classNames[i++]) {
 5773:                          // Check each className given, space separated list
 5774:                          if (self.hasClass(className)) {
 5775:                              self.removeClass(className);
 5776:                          } else {
 5777:                              self.addClass(className);
 5778:                          }
 5779:                      }    // Toggle whole class name
 5780:                  } else if (value === undefined || type === 'boolean') {
*****

***** JQUERY\RD\52.js
 5948:              // Determine event propagation path in advance, per W3C events spec (#9951)
 5949:              if (!onlyHandlers && !special.noBubble && !jQuery.isWindow(elem)) {
***** JQUERY\RD\ORIGINAL.JS
 5967:              // Determine event propagation path in advance, per W3C events spec (#9951)
 5968:              // Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
 5969:              if (!onlyHandlers && !special.noBubble && !jQuery.isWindow(elem)) {
*****

***** JQUERY\RD\52.js
 6197:          allTypes = '*/'.concat('*'),
 6198:          originAnchor = document.createElement('a');
***** JQUERY\RD\ORIGINAL.JS
 6217:          allTypes = '*/'.concat('*'),
 6218:          // Anchor tag for parsing the document origin
 6219:          originAnchor = document.createElement('a');
*****

***** JQUERY\RD\52.js
 7032:          accepts: { script: 'text/javascript, application/javascript, ' + 'application/ecmascript, application/x-ecmascript' },
 7033:          contents: {},
 7034:          converters: {
***** JQUERY\RD\ORIGINAL.JS
 7053:          accepts: { script: 'text/javascript, application/javascript, ' + 'application/ecmascript, application/x-ecmascript' },
 7054:          contents: { script: /\b(?:java|ecma)script\b/ },
 7055:          converters: {
*****

***** JQUERY\RD\52.js
 7204:              callback = params;
 7205:          } else if (params && typeof params === 'object') {
***** JQUERY\RD\ORIGINAL.JS
 7225:              callback = params;
 7226:              params = undefined;    // Otherwise, build a param string
 7227:          } else if (params && typeof params === 'object') {
*****

